# Allow all visitors to reveal source credentials

Version 1 has no identity or authorization and runs only within the organization's trusted private network. We accept the risk of allowing every visitor to reveal and replace bearer tokens and API keys because unrestricted shared administration is a deliberate first-version constraint; exposing this platform outside that network requires revisiting this decision.

# Use React, TypeScript, and Vite

The browser application will use React, TypeScript, and Vite, with its production build bundled as static assets inside the Spring Boot JAR. Node.js is required only for development and builds; the Windows Server deployment requires Java 21 but no Node.js runtime.

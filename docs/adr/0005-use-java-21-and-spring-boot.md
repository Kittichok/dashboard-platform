# Use Java 21 and Spring Boot

The dashboard server will run on Windows Server using Java 21 and Spring Boot. Java 21 provides a supported LTS runtime for the REST proxy, SQLite persistence, and web API, while Node.js 15 was rejected because it reached end of life on April 6, 2021.
